[project]
name = "sms-agent"
version = "0.1.0"
description = "Semantic Memory Steward (SMS) MVP"
authors = [{name = "Antigravity", email = "antigravity@example.com"}]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "strands-agents>=1.54.0",
    "boto3",
    "pydantic"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "python-dotenv",
    "streamlit"
]
